export default class ListItemMetadata {
  constructor({ functions, names, icons, colors }) {
    this.functions = functions;
    this.names = names;
    this.icon = icons;
    this.colors = colors;
  }
}
